import Header from "../Components/Header";

const Home = () => {
  return (
    <div>
      {/* header section */}
      <Header></Header>
      {/* sidebar + cards */}
    </div>
  );
};

export default Home;
